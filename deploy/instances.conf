# Single source of truth for every systemd-managed instance on the server.
# Read by deploy/_apply-update.sh, deploy/qbank-rollback.sh,
# deploy/qbank-service-ctl.sh, and admin_app.py — edit this one file when
# adding/removing an instance rather than touching each script.
#
# name:label:app_dir:service:user:port
ncms:NCMS:/opt/qbank/app:qbank.service:qbank:5000
chs:CHS:/opt/qbank-chs/app:qbank-chs.service:qbank-chs:5001
